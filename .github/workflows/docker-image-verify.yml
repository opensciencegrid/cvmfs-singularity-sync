name: Docker images verification
# This workflow is triggered on pushes to the repository.
on: [push, pull_request]


jobs:
  build:
    #name: Greeting
    # This job runs on Linux
    runs-on: ubuntu-latest
#    env:
#      docker_images_path: ./docker_images.txt

    steps:
#      - name: Hello world
       - uses: actions/checkout@v1
       - run: cp ./docker_images.txt ./.github/actions/docker-images-verification
       - uses: ./.github/actions/docker-images-verification
#         with:
#           docker-images-file: './docker_images.txt'
#           id: hello
        # This step prints an output (time) from the previous step's action.
#      - name: Echo the greeting's time
#        run: echo 'The time was ${{ steps.hello.outputs.time }}.'